# Normalise line endings so the repo stays consistent between Windows and
# the Linux builders Vercel runs on.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.mp4 binary
*.svg text eol=lf
